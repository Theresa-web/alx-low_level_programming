#include "main.h"
#include <stdio.h>
/**
 * swap_int -swaps the values of two integers
 * main - swap value
 * @a: integer to swap
 * @b: interger to swap
 */

void swap_int(int *a, int *b);
int main(void)
{
	int c = *a;
	*a = *b;
	*b = c;
}
