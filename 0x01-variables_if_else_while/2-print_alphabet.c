#include <stdio.h>
/**
 * main - Entry point
 * Return: Always 0 (Sucess/correct)
 */
int main(void)
{
	char sam;

	for (sam = 'a'; sam <= 'z'; samm++)
	{
		putchar(sam);
	}
	putchar("\n");
	return (0);
}
